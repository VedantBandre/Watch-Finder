# Leave empty for local development, where Vite proxies /api to FastAPI.
# Set this to the Render service URL in Vercel (without a trailing slash).
VITE_API_BASE_URL=https://your-watch-finder-api.onrender.com
